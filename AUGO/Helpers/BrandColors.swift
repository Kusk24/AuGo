import SwiftUI

extension Color {
    enum Brand {
        static let primary        = Color("BrandPrimary")
        static let coin           = Color("BrandCoin")
        static let tabBackground  = Color("BrandTabBackground")
    }
}
